# shamefully-hoist=true hoists all packages to root node_modules.
# Required for React Native: native build tooling (Gradle, Xcode)
# resolves modules from the project root via Node's require().
shamefully-hoist=true
strict-peer-dependencies=true
auto-install-peers=true
